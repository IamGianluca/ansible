  - name: Install System Packages 1
    become: true
    ansible.builtin.apt:
      name: ['build-essential', 'cmake']

  - name: Install System Packages 2
    become: true
    ansible.builtin.apt:
      name: ['unzip', 'curl', 'python3', 'python3-dev', 'python3-pip']

